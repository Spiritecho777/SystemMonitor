[package]
name = "SystemMonitor"
version = "1.0.0"
edition = "2021"

[dependencies]
fltk = { version = "1", features = ["fltk-bundled"] }
sysinfo = "0.30"
group = "0.14.0"

[profile.release]
opt-level = 3
lto = true
strip = true